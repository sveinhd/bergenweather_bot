{
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@atproto/api": "^0.6.20",
    "cron": "^3.1.0",
    "dotenv": "^16.3.1",
    "process": "^0.11.10"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
